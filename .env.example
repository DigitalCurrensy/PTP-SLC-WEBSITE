# PTP SLC Events — no runtime secrets live in this repo.
# The public site does not need environment variables.
#
# If lead forms are wired later, the Google Apps Script URL
# belongs in the live site's config.js (window.PTP.leadEndpoint),
# not in a committed .env.

# PTP_LEAD_ENDPOINT=
